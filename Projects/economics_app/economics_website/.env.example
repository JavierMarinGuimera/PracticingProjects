JWT_SECRET=change-this-long-random-secret
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: real Python service endpoint. If unset, /api/news/python-mock is used.
PYTHON_NEWS_API_URL=

# Optional: real Stripe checkout. If unset, checkout is mocked and activates the subscription locally.
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
STRIPE_WEBHOOK_SECRET=
